version: '3.4'
services:
  dense-seg:
    image: 'rueberger/dense-correspondence'
    networks:
      - core
    volumes:
      - '/data/dense_seg:/home/andrew/code/data_volume'
      - 'torch:/home/andrew/torch'
    ports:
      - '172.30.0.0:9999:8888'


networks:
  core:
    driver: overlay
    attachable: true

volumes:
  torch:
